include: package:lints/recommended.yaml

analyzer:
  exclude:
    - doc/api/**

linter:
  rules:
    - always_declare_return_types
    - directives_ordering
    - prefer_final_locals
    - prefer_single_quotes
    - unawaited_futures
